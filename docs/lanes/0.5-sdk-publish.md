# Lane 0.5-SDK-Publish — ship `pip install dejima-sdk` / `npm install @dejima/sdk`

You are the **SDK-Publish** agent. The Python + TS SDKs and `sdk-publish.yml` exist
and are dormant; make publishing real and reliable. Fully independent of the other
0.5 lanes.

**Read first:** `.github/workflows/sdk-publish.yml`, `sdk/python/`, `sdk/ts/`,
`sdk/openapi_parity.py`, `docs/roadmap.md` (Versioning map → 0.5).

**Scope:**
1. **Harden `sdk-publish.yml`** — confirm the tag-driven (`v*`) publish: Python →
   PyPI (`PYPI_API_TOKEN`), TS → npm (`NPM_TOKEN`); upload step skips cleanly when a
   secret is absent (already designed dormant). Add a `workflow_dispatch` so it can be
   run manually for an existing tag without re-tagging.
2. **Version stamping** — ensure the published package version tracks the git tag
   (e.g. `v0.4.0` → `0.4.0`) rather than a hardcoded `0.1.0` in `pyproject.toml` /
   `package.json`; keep the "0.x — may change" note.
3. **`openapi_parity.py` fix** — it currently exits 0 when PyYAML is missing; make a
   missing dep a hard failure (exit non-zero) so CI can't false-pass.
4. **Install docs** — confirm the `pip`/`npm` snippets on `build.html` + `api.html`
   match the published package name.

**Blocked on the owner** (flag, don't fake): the `dejima` names must be claimed on
PyPI + npm and `PYPI_API_TOKEN`/`NPM_TOKEN` added as repo secrets before a real
publish. Until then, wire everything so the first tag-with-secrets publishes.

**Owns:** `.github/workflows/sdk-publish.yml`, `sdk/`, `sdk/openapi_parity.py`,
SDK snippets in `build.html`/`api.html`. **Don't touch:** daemon Go code.

**Workflow:** branch `feat/sdk-publish`; lint the spec + run the SDK tests; PR to
`master`. *(Note: pushing `.github/workflows/**` needs the gh `workflow` token
scope — see the team note if the push is rejected.)*
